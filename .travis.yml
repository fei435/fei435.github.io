sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - source # build source branch only
git:
  depth: 1
  submodules: true
env:
  global:
    - GITHUB_TOKEN: "${GITHUB_TOKEN}"
before_install:
  - npm install -g hexo-cli
install:
  - npm install
script:
  - echo "$GITHUB_TOKEN"
  - hexo generate
after_success:
  - git config --global user.name 'fei435'
  - git config --global user.email 'yyy435@gmail.com'
  - sed -in-place -e "s/git@/https:\/\/${GITHUB_TOKEN}@/" _config.yml
  - sed -in-place -e "s/github.com:fei435/github.com\/fei435/" _config.yml
  - cat _config.yml
  - hexo deploy
